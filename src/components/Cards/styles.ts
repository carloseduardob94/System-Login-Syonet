import styled from "styled-components";

export const Container = styled.div`
  background-color: #e0dada;
  max-width: 30rem;
  width: 100%;
  height: 7rem;
  border: 2px solid #661717;
  border-radius: 8px;
  text-align: center;
  box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;

  > h2 {
    font-size: 1.3rem;
    padding: 0.2rem;
  }

  > p {
    font-size: 1.1rem;
  }
`